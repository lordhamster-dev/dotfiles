# dotfiles

## Install Font

```bash
sudo pacman -S ttf-cascadia-code-nerd
```

## Install packages

```bash
sudo pacman -S clash neovim starship task tmux yazi zsh fzf fd ripgrep zoxide keyd btop cliphist mpv obs-studio hyprpaper hyprlock rofi rofi-emoji
```

```bash
./install.sh
```

## Change shell

```bash
chsh -s $(which zsh)
```
